// import { generateApiClient } from "@app/utils/apiUtils";
// const itunesApi = generateApiClient("itunes");

// export const getRecommendations = async () => {
//   const res = await itunesApi.get(`search?term=`);
//   console.log("res", res);
//   const getData = response => {
//     if (!response.ok) {
//       console.log(res.data);
//       return [];
//     }

//     const recomendations = ["arijit", "anuv"];
//     return recomendations;
//     // return response.data.filter(({ name }) =>
//     //   recomendations.includes(name).map(({ id, name }) => ({ id, name }))
//     // );
//   };

//   return getData(res);
// };

/******* work on recommendations later   **********/
